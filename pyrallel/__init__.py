def joke():
  return (u'Wenn ist das Nunst\u00fcck git und Slotermeyer? Ja! ... '
    u'Beiherhund das Oder die Flipperwaldt gersput.')


def uploadHelper(path, data):
  # save to file? (or zip from memory)
  # zip
  # upload (pydrive)


def completionChecker(path, partitions):
  # are all files in path

def importHelper():
  # import all partitions

def compilationHelper():
  # importHelper
  # compile
  # uploadHelper

def delHelper():
  # delete all partitions, leave whole


def pyrallel(path, array, function, partitions, postCompile=False, finDel=False):
  # array is array? 
  # ask which partition number to run (int)
  # slice

  # outArr = run function on arr (expect arr return?) # might want to handle other outputs in the future
  # upload helper

  # completion checker
     # deletion checker
